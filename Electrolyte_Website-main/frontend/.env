# For development with production backend (requires CORS to be configured)
REACT_APP_API_BASE_URL=https://electrolyte-website.onrender.com/api

# For development with local backend, comment out the line above
# and make sure your backend is running on localhost:5000
